define(["editor-game-object-container"], function(EditorGameObjectContainer) {
  var PlayerShipOption = EditorGameObjectContainer.extend({
    init: function() {
      this._super();
    },

    editorStart: function() {
      
    },

    editorUpdate: function(delta) {
      
    },

    onCollide: function(other) {

    }

  });

  return PlayerShipOption;
});
